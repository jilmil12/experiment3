# 🧪 Experiment 3: User Login System (Select & Verify)

## 🚀 How to Run

### 1️⃣ Install dependencies
bash
npm install
2️⃣ Make sure your database is ready
Use the same database as Experiment 2 (userdb) with the users table.

3️⃣ Start the server
bash
Copy code
npm start
or

bash
Copy code
node server.js
You should see:

arduino
Copy code
✅ Database connected!
✅ Server running on http://localhost:3000
4️⃣ Open in your browser
Go to:
👉 http://localhost:3000

Enter your email and password (from the user you registered in Experiment 2).

If correct:
✅ You’ll be redirected to the dashboard.

If incorrect:
❌ You’ll see an error message (“Incorrect password” or “User not found”).
